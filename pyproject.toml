[project]
name = "email-categorise"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv",
    "msal",
    "msal-extensions",
    "requests",
    "toml",
    "pytest>=9.0.2",
]
